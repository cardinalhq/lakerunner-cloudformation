# Grafana Setup configuration for Lakerunner deployment
# This file contains configuration for the Grafana database setup stack

# Database configuration
database:
  name: "lakerunner_grafana"
  user: "grafana"

# This stack has minimal configuration since it primarily creates database resources
# Most settings are inherited from the CommonInfra stack via imports